version: "3.9"
services:
  mongo:
    image: mongo
    restart: always
    env_file:
      - ./.env

  server:
    build:
      context: ./server
      dockerfile: Dockerfile-prod
    volumes:
      - ./server:/server
    links:
      - mongo
    env_file:
      - ./.env
    networks:
      - web_nw

  client:
    build:
      context: ./client
      dockerfile: Dockerfile-prod
    volumes:
      - ./client:/client
      - /client/node_modules
      - build-react:/client/build
    env_file:
      - ./.env

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile.prod
    volumes:
      - build-react:/web
    depends_on:
      - server
      - client
    ports:
      - "80:80"
      - "5000:5000"
    networks:
      - web_nw

networks:
  web_nw:
    driver: bridge
volumes:
  build-react:
